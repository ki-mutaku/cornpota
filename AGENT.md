# AGENT.md

## 1. このファイルの目的

このファイルは、Godot
4.xで開発する2Dゲーム「コンポタマスター」において、CodexなどのAI Coding
Agentが安全にチーム開発へ参加するための作業ルールを定義する。

AI Agentは、実装を開始する前に必ず以下を確認すること。

1.  `AGENT.md`
2.  `SPEC.md`
3.  変更対象に関連する既存Scene・Script・Resource

`SPEC.md` をゲーム仕様の正とし、`AGENT.md` を開発時の行動規範とする。

------------------------------------------------------------------------

## 2. プロジェクト概要

- Engine: Godot 4.x
- Game Type: 2D
- Language: GDScript
- Version Control: Git / GitHub
- Main Input: Touch / Swipe / Drag
- Team Size: 3
- Development Style: Sceneを小さく分割した並行開発

本ゲームは、プレイヤーが2Dのキッチンで材料や調理器具を操作し、コンポタを作って客へ提供する料理シミュレーションゲームである。

詳細なゲーム仕様は `SPEC.md` を参照すること。

------------------------------------------------------------------------

## 3. 最重要ルール

### 3.1 SPEC.mdを勝手に変更しない

実装と `SPEC.md`
が矛盾する場合、実装側へ勝手に仕様を追加して解決してはならない。

特に `SPEC.md` の「未確定仕様」に記載されている項目について、AI
Agentが独自判断で仕様を確定してはならない。

必要な仕様が未確定の場合は、

1.  現在の仕様では何が決められないか
2.  実装するために何を決める必要があるか
3.  必要なら選択肢

をユーザーへ提示し、確認を求める。

### 3.2 担当外のファイルを不要に変更しない

タスク達成に必要な最小範囲のみ変更する。

例えばPlayerの移動処理を依頼された場合、理由なく以下を変更しない。

- Customer
- HUD
- Recipe
- Stage
- 他メンバーが担当するScene

変更が必要な場合は、その理由を説明する。

### 3.3 同じ `.tscn` の同時編集を避ける

Godot Sceneは小さく分割して管理する。

複数機能を1つのSceneへ集約しない。

特に `kitchen.tscn`
は統合Sceneであり競合しやすいため、明示的に依頼されていない限り変更を最小限にする。

### 3.4 既存Scene Architectureを尊重する

AI Agentの判断だけで大規模なScene再構成を行わない。

既存のScene、Script、Resourceを確認し、現在の構造に沿って実装する。

------------------------------------------------------------------------

## 4. 作業開始時の手順

コードを書く前に以下を行う。

### Step 1：仕様を読む

`SPEC.md` のうち、依頼された機能に関連する部分を確認する。

### Step 2：既存実装を確認する

変更対象とその周辺の、

- `.tscn`
- `.gd`
- `.tres`
- `.res`
- Project Settings
- Input Map

を必要な範囲で確認する。

### Step 3：変更範囲を決める

どのファイルを変更・追加する必要があるか整理する。

既存ファイルへの変更が不要なら、新規ファイルだけで実装できないか検討する。

### Step 4：実装する

既存Architectureと本ファイルのルールに従って実装する。

### Step 5：確認する

可能な範囲で、

- 構文エラー
- NodePath
- Signal
- Resource参照
- Scene参照
- 型
- 依存関係

を確認する。

------------------------------------------------------------------------

## 5. Scene設計ルール

GodotのScene Editorを使用することを前提とする。

すべてをGDScriptから動的生成する設計へ変更しない。

基本構成：

``` text
res://
├── scenes/
│   ├── game/
│   ├── player/
│   ├── cooking/
│   ├── ingredients/
│   ├── customers/
│   └── ui/
├── scripts/
│   ├── game/
│   ├── player/
│   ├── cooking/
│   ├── ingredients/
│   ├── customers/
│   └── ui/
├── resources/
├── assets/
├── project.godot
├── SPEC.md
└── AGENT.md
```

### Sceneは小さく保つ

Player、Pot、Mixer、Customer、HUDなどは可能な限り独立Sceneとして扱う。

例：

``` text
Kitchen
├── Player      → player.tscn
├── Pot         → pot.tscn
├── Mixer       → mixer.tscn
├── Customers
└── HUD         → hud.tscn
```

### SceneとScriptを分離する

例：

``` text
scenes/cooking/pot.tscn
scripts/cooking/pot.gd
```

`.tscn` は主に以下を担当する。

- Node構造
- Sprite
- Collision
- 位置
- Animation
- Inspector設定
- Signal接続

`.gd` は主に以下を担当する。

- 状態
- ゲームロジック
- 計算
- 入力処理
- Scene間通信

------------------------------------------------------------------------

## 6. `.tscn` 編集ルール

AI Agentによる `.tscn` の作成・編集は禁止しない。

ただし以下を守る。

- 必要のないNodeを追加しない
- Node名を理由なく変更しない
- 既存Node階層を理由なく変更しない
- Scene UIDやResource参照を不用意に書き換えない
- 大量のScene変更を1タスクに混ぜない
- `.tscn` を編集した場合は、変更したことを作業結果で明示する

Scene Editorでの視覚的調整が必要な、

- Sprite位置
- Collision範囲
- UI位置
- Animation
- 操作感

については、コードだけで完全に正しいと断定しない。

人間がGodot Editorで最終確認する前提とする。

------------------------------------------------------------------------

## 7. GDScriptルール

Godot 4.x向けGDScriptを使用する。

可能な範囲で型を明示する。

例：

``` gdscript
var temperature: float = 0.0
var ingredients: Array[IngredientData] = []
```

関数は1つの責務を持たせる。

悪い例：

``` gdscript
func update_game():
    # Player
    # Cooking
    # Customer
    # UI
    # Score
```

機能ごとに分割する。

Node参照を大量の絶対NodePathへ依存させない。

可能な場合は、

- `@export`
- Signal
- Resource
- Scene単位の責務分離

を利用する。

------------------------------------------------------------------------

## 8. Scene間通信

Scene同士を強く結合しない。

例えばPlayerからHUD内部のLabelを直接変更するような実装は避ける。

悪い例：

``` gdscript
get_node("/root/Kitchen/UI/HUD/ScoreLabel").text = str(score)
```

代わりにSignalやゲーム進行を管理するNodeを介する。

``` text
Customer
    │
    │ soup_served
    ↓
GameManager
    │
    ├── Satisfaction更新
    │
    └── Signal
          ↓
         HUD
```

------------------------------------------------------------------------

## 9. Resource設計

材料、レシピ、Customer、Stageなど、同じ構造で内容だけが異なるデータは、可能な限りResourceとして管理する。

例えば材料ごとに、

``` text
corn.gd
milk.gd
butter.gd
```

とロジックを複製するより、

``` text
IngredientData
├── id
├── display_name
├── sweetness
├── richness
└── texture
```

のようなデータ構造を利用する。

ただし、まだ `SPEC.md` で確定していないデータ値をAIが勝手に設定しない。

------------------------------------------------------------------------

## 10. Inputルール

本ゲームはタッチ操作を基本とする。

主要操作：

- Tap
- Swipe
- Drag

キーボード操作を前提としたシステムを新規実装しない。

PC上での開発確認にマウス入力を使用する場合は、タッチ操作と同じ入力モデルとして扱える構造を優先する。

具体的な調理操作は `SPEC.md` に従う。

------------------------------------------------------------------------

## 11. 調理システムの注意

現在想定されている操作には以下がある。

- お玉をドラッグして鍋を混ぜる
- コンロのツマミをドラッグして火力を変える
- ミキサーでコーンをすりつぶす
- 牛乳を注ぐ
- 砂糖などの材料を入れる

調理成功・失敗の具体的な判定条件など、`SPEC.md`
で未確定のものを実装する必要が出た場合は、仮仕様を本番仕様として埋め込まない。

------------------------------------------------------------------------

## 12. UIルール

ゲームロジックをUI Sceneへ持たせすぎない。

HUDは基本的にゲーム状態を表示する責務を持つ。

例：

``` text
GameManager
    ↓ Signal
HUD
    ↓
表示更新
```

HUD自身が満足度計算やステージクリア判定を行わない。

------------------------------------------------------------------------

## 13. Git・チーム開発ルール

AI AgentはGit操作を行う場合、既存のユーザー変更を尊重する。

### 禁止事項

明示的な指示なしに以下を実行しない。

``` bash
git reset --hard
git clean -fd
git checkout -- .
git restore .
git push --force
```

既存の未コミット変更を勝手に破棄しない。

### Branch

原則としてfeature branchで作業する。

例：

``` text
feature/player
feature/cooking
feature/ui
feature/customer
```

`main` へ直接実装をpushしない。

### Commit

1つのcommitに無関係な変更を大量に混ぜない。

例：

``` text
feat: add pot mixing interaction
fix: prevent duplicate ingredient insertion
refactor: separate customer evaluation logic
```

------------------------------------------------------------------------

## 14. 他メンバーの作業を守る

作業開始時にGit差分が存在する場合、それを自分の変更だと仮定しない。

他メンバーまたはユーザーによる変更の可能性がある。

そのため、

- 勝手に削除しない
- 勝手にrevertしない
- 理由なく整形し直さない
- タスクと無関係なファイルを変更しない

を守る。

------------------------------------------------------------------------

## 15. 未確定仕様の扱い

`SPEC.md` の未確定仕様は意図的に未確定である。

AI Agentは、

「実装しやすいから」

という理由だけで仕様を決めてはならない。

### 実装に必要でない場合

未確定のままにする。

### 実装に必要な場合

ユーザーへ確認する。

### 仮実装を明示的に依頼された場合

後から変更しやすい構造にする。

例えば、

``` gdscript
const BASE_SUCCESS_RATE := 0.8
```

を複数Scriptへ直接埋め込むのではなく、Resourceや設定値として分離する。

------------------------------------------------------------------------

## 16. オーバーエンジニアリングを避ける

本プロジェクトはハッカソン向けゲームである。

将来必要になるかもしれない機能のために、大規模な抽象化や複雑なフレームワークを先に作らない。

優先順位：

``` text
1. 遊べる
2. チームで統合できる
3. 読みやすい
4. 拡張しやすい
5. 高度な抽象化
```

最小ゲームループの完成を優先する。

------------------------------------------------------------------------

## 17. 実装優先順位

### Priority 1

``` text
Player操作
↓
材料を取得
↓
調理
↓
コンポタ完成
↓
客へ提供
↓
満足度獲得
↓
制限時間終了
↓
Result
```

### Priority 2

- 複数材料
- 客の好み
- コンポタ評価
- 調理成功・失敗
- ステージノルマ
- ボス

### Priority 3

- レシピ発見
- 料理レベル
- 材料アンロック
- Animation
- 演出
- 効果音

Priority 1を壊してまでPriority 2・3を実装しない。

------------------------------------------------------------------------

## 18. テスト・確認

変更後は可能な範囲で以下を確認する。

- GodotがSceneを読み込める
- GDScriptに構文エラーがない
- 存在しないNodePathを参照していない
- Signal名と接続先が一致している
- Resource参照が壊れていない
- 既存機能を不必要に壊していない

Godotを実行できない環境では、実行確認を行ったと偽らない。

その場合は、

``` text
静的確認済み
Godot Editorでの実行確認が必要
```

と明示する。

------------------------------------------------------------------------

## 19. 作業完了時の報告

作業終了時は簡潔に以下を報告する。

### 変更内容

何を実装・変更したか。

### 変更ファイル

例：

``` text
scripts/cooking/pot.gd
scenes/cooking/pot.tscn
```

### 確認内容

何を確認したか。

### 人間による確認が必要な項目

例：

- CollisionShape2Dの大きさ
- ドラッグ操作の感度
- Sprite位置
- 実機でのタッチ操作

### 未解決事項

仕様決定が必要なものがあれば記載する。

------------------------------------------------------------------------

## 20. Definition of Done

AI Agentがタスクを完了したと判断する条件：

- 依頼された機能が実装されている
- `SPEC.md` と矛盾していない
- 未確定仕様を勝手に確定していない
- タスク外のファイルを不必要に変更していない
- 既存のユーザー・他メンバーの変更を破壊していない
- 可能な範囲でエラー確認を行っている
- `.tscn` を変更した場合、その旨を報告している
- 人間が確認すべき項目を明示している

------------------------------------------------------------------------

## 21. 判断に迷った場合

以下の順で判断する。

``` text
SPEC.md
   ↓
AGENT.md
   ↓
既存実装
   ↓
タスクの指示
   ↓
ユーザーへ確認
```

既存仕様を推測して大きく変更するより、確認を取ることを優先する。

ただし、仕様に影響しない小さな実装詳細については、既存コードとの一貫性を優先して合理的に判断してよい。
